<%= form_with(model: course, local: true) do |f| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
        <% course.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-inputs">
      <%= f.text_field :title, label: 'Titre:', placeholder:"Le titre du Cours" %>
      <%= f.rich_text_area :content, label: 'Contenu:', class: "customized-min-height", placeholder: "Saissir le contenu ..." %>
    </div>

    <div class="row my-2">
      <div class="col-5  col-md-3 col-lg-3">
        <div class="input-form">
          <span class="select">
            <%= f.select :material_id,
              @materials.map { |m| [m.title, m.id] },
              placeholder:"la matiere",
               hide_label: true %>

          </span>
        </div>
      </div>
      <div class="col-5 col-md-3 col-lg-3">
        <div class="input-form">
          <span class="select">
            <%= f.select :level_id,
              @levels.map { |l| [l.title, l.id] },
              placeholder:"Niveau",
               hide_label: true %>
          </span>
        </div>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit "Créer le Cours",  class: "btn btn-primary" %>
    </div>
<% end %>
